{
    "name": "ut-example",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "@sap/cds": "^4",
        "cds-mysql": "^0.1.4",
        "express": "^4"
    },
    "devDependencies": {
        "jest": "^26.6.3",
        "sqlite3": "^5"
    },
    "jest": {
        "testEnvironment": "node"
    },
    "scripts": {
        "start": "node index.js",
        "test": "jest",
        "deploy": "cds-mysql deploy"
    },
    "cds": {
        "requires": {
            "db": {
                "kind": "mysql"
            },
            "mysql": {
                "impl": "cds-mysql"
            }
        }
    }

}
